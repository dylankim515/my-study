#include <stdio.h>

int main()
{
	int x=10, y=8;

	printf(" x > y의 결과값 : \t\t %d\n", x>y);
	printf(" x < y의 결과값 : \t\t %d\n", x<y);
	printf(" x == y의 결과값 : \t\t %d\n", x==y);
	printf(" x != y의 결과값 : \t\t %d\n", x!=y);
	printf(" x >= y의 결과값 : \t\t %d\n", x>=y);
	printf(" x <= y의 결과값 : \t\t %d\n", x<=y);

	return 0;
}

